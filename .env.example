# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env-schema.ts"
# should be updated accordingly.

# Domain công khai, dùng cho metadataBase / sitemap.xml / robots.txt / OG card.
# Tuỳ chọn: KHÔNG nằm trong env-schema.js nên thiếu cũng không làm build/dev chết.
# Thứ tự fallback (src/lib/site.ts): biến này → URL Vercel tự cấp → domain
# production mặc định (khi NODE_ENV=production) → http://localhost:3000.
# Chỉ cần set khi chạy sau một domain riêng.
# NEXT_PUBLIC_SITE_URL="https://agentforge.fptsmartcloud.vn"

DATABASE_URL="postgresql://postgres:password@127.0.0.1:5432/agentforge"
PYTHON_API_URL="http://127.0.0.1:8444"
FALLBACK_TO_FIXTURE="true"
# Trần là 10, không phải 20 mà backend chấp nhận: 20 trang mất ~4 phút và vượt
# budget fetch 3 phút của chính backend, nên lượt crawl chết sau khi đã chờ rất
# lâu. 10 trang về trong ~2 phút.
CRAWL_MAX_PAGES="5"

# --- Voice (gateway của nền tảng FPT Voice Agent) ---
# Browser nối THẲNG tới gateway này: WebSocket không proxy qua Next được, và
# lệnh DELETE lúc đóng tab phải phát từ chính browser (fetch keepalive). Gateway
# giữ API key phía server nên URL này không lộ bí mật nào.
# Thiếu biến này thì giao diện nói "voice chưa cấu hình" thay vì để một nút chết.
# Muốn link demo chia sẻ gọi được từ máy khác thì trỏ vào tunnel của gateway,
# không phải localhost.
NEXT_PUBLIC_VOICE_GATEWAY_URL="http://localhost:8787"
# Tên Agent Profile trên gateway (`agents.local.yaml` / `agents.generated.yaml`).
# KHÔNG hardcode: profile khác nhau theo từng máy chạy gateway — `longchau` mà
# handoff ghi không tồn tại trên gateway đang chạy ở đây, nó nạp `forge-…`.
# Gửi sai tên thì gateway trả 404 `unknown_profile`.
NEXT_PUBLIC_VOICE_PROFILE="longchau"
